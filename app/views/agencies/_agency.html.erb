
  <div class="agency-card">
    <%= cl_image_tag(agency.photo, height:  100, crop: :pad)  unless agency.photo.nil? %>

    <hr class="separator-card">
          <h2><%= agency.name %></h2>

    <p> <%= agency.short_description %></p>
    <div class="stars">
      <% agency.review_mean.to_i.times do %>
        <i class="fa fa-star"></i>
      <% end %>
    </div>
  </div>
